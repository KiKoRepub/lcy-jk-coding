<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dee.mapper.ToolMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO tool (tool_name, description, class_name, input_schema, enabled, category, created_at, updated_at)
        VALUES
        <foreach collection="list" item="tool" separator=",">
            (#{tool.toolName}, #{tool.description}, #{tool.className},
             #{tool.inputSchema}, #{tool.enabled}, #{tool.category}, #{tool.createdAt}, #{tool.updatedAt})
        </foreach>
    </insert>

    <select id="selectEnabledTools" resultType="org.dee.entity.SQLTool">
        SELECT * FROM tool WHERE enabled = 1 ORDER BY created_at DESC
    </select>

</mapper>
